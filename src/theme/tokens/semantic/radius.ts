import { radius } from "@/theme/tokens";

export const semanticRadius = {
  page: radius.none,
  card: radius.lg,
  button: radius.md,
  input: radius.md,
  modal: radius.xl,
  dialog: radius.xl,
  avatar: radius.full,
  badge: radius.full,
} as const;
