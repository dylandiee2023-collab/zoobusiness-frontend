import { iconSizes } from "@/theme/tokens";

export const semanticIconSizes = {
  navigation: iconSizes.lg,
  toolbar: iconSizes.md,
  button: iconSizes.md,
  input: iconSizes.sm,
  card: iconSizes.lg,
  avatar: iconSizes.xl,
} as const;
