import { borders } from "@/theme/tokens";

export const semanticBorders = {
  card: borders.width.thin,
  input: borders.width.thin,
  divider: borders.width.hairline,
  focus: borders.width.medium,
} as const;
