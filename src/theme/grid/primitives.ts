import type {
  GridMap,
} from "./types";

export const grids: GridMap = {
  mobile: {
    columns: 4,
    gap: 12,
  },

  mobileLg: {
    columns: 4,
    gap: 16,
  },

  tablet: {
    columns: 8,
    gap: 20,
  },

  laptop: {
    columns: 12,
    gap: 24,
  },

  desktop: {
    columns: 12,
    gap: 32,
  },

  wide: {
    columns: 12,
    gap: 40,
  },
};